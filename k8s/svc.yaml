apiVersion: v1
kind: Service
metadata:
  name: abayomi-service
  namespace: yomisp
spec:
  type: NodePort
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 8082
      nodePort: 9009
      targetPort: 5000
